<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androiddemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidDemo">
        <activity android:name=".MaterialButtonActivity"></activity>
        <activity android:name=".SVGAPlayActivity" />
        <activity android:name=".ConstraintLayoutDemoActivity" />
        <activity android:name=".MediaScannerConnectionActivity" />
        <activity android:name=".wxapi.WXEntryActivity" />
        <activity android:name=".ShareToWechatActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DownloadImageJavaActivity" />
        <activity android:name=".DirDifferenceActivity" />
    </application>

</manifest>